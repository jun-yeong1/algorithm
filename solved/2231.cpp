#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  // 입력받은 수 자리수 구하기
  int len = n;
  int count = 0;
  while (len != 0) {
    len /= 10;
    count++;
  }
  //가장 작은 생성자
  int result = 1-;
  for (int i = (count * 9); i < 0; i--) {
    int min_n = n - i;
  }
}